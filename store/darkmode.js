const state = () => ({
    // darkmode: null,
})

const getters = {
    // dark(state) {
    //     return state.darkmode
    // }
}

const mutations = {
    // SET_DARK (state, darkmode) {
    //     state.darkmode = darkmode
    // },
}

const actions = {
    // nuxtServerInit({commit}) {
    //     let date = new Date();
    //     let bolDark = (date.getHours() > 22 || date.getHours() < 6);
    //     commit("SET_DARK", bolDark)
    // },
    // setDark({commit}, darkmode) {
    //     commit("SET_DARK", darkmode)
    // },
}

export default {
    state,
    getters,
    mutations,
    actions
}